{
    "name": "dkfds-react",
    "repository": {
        "type": "git",
        "url": ""
    },
    "version": "1.0.0",
    "description": "A React component library for Fælles design system (FDS) for use in public faced projects in Denmark.",
    "main": "dist/cjs/index.js",
    "module": "dist/esm/index.js",
    "files": [
        "dist"
    ],
    "scripts": {
        "build": "npm run clean && npm run prettify && npm run build:esm && npm run build:cjs",
        "clean": "rm -rf dist/",
        "prettify": "prettier --write src/",
        "reinstall": "rm -rf node_modules/ && rm -f package-lock.json && npm install",
        "build:esm": "tsc",
        "build:cjs": "tsc --module CommonJS --outDir dist/cjs"
    },
    "author": "Jonas Ladefoged Holm",
    "license": "MIT",
    "devDependencies": {
        "@types/node": "^18.11.0",
        "@types/react": "^18.0.21",
        "@types/react-dom": "^18.0.6",
        "react": "^18.2.0",
        "react-dom": "^18.2.0",
        "typescript": "^4.8.4",
        "prettier": "^2.7.1"
    },
    "peerDependencies": {
        "react": "^18.2.0",
        "react-dom": "^18.2.0"
    }
}
